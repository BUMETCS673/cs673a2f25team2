spring:
  ai:
    openai:
      # Here we use Alibaba's Qwen model, if you need to modify to other models, please adjust the configuration yourself
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      # Fill in the key applied from Alibaba Bailian large model platform (https://bailian.console.aliyun.com/?tab=model#/model-market)
      api-key: xxx
      # This configuration is for "chat model"
      chat:
        options:
          # Model name (here we use Qwen-Plus, can be replaced with what you want to use)
          model: qwen-plus

  datasource:
    url: jdbc:mysql://localhost:3306/db_health?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: zhiyao1156501 # change to yours
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

  # redis:
  #   port:6379
  #   host:localhost


# Debug report
logging:
  level:
    com.shanzhu: debug

# Logical delete
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleted # Global logical delete entity field name (since 3.3.0, can ignore step 2 after configuration)
      logic-delete-value: 1 # Logical deleted value (default is 1)
      logic-not-delete-value: 0 # Logical not deleted value (default is 0)
  type-aliases-package: com.shanzhu.*.entity

# JWT configuration
# Production environment recommends using environment variables: export JWT_SECRET=your-secret-key
jwt:
  secret: ${JWT_SECRET:y3aX2K8/4TdF6zBmQv7hPwScN9jLgRtA+UoWxEiYlDn5fMpGqHtJkKuV}

# CORS configuration
# Production environment recommends using environment variables: export CORS_ALLOWED_ORIGINS=http://yourdomain.com
cors:
  allowed-origins: http://localhost:5173,http://localhost:3000

# Data encryption configuration
# Production environment recommends using environment variables: export ENCRYPTION_KEY=your-16-byte-key
# Note: Key must be 16 bytes (AES-128), will be automatically adjusted if length is incorrect
encryption:
  key: ${ENCRYPTION_KEY:HealthSystem2024Key}

